# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

includes:
  lib:
    taskfile: ../../.tasks.lib.yml
    internal: true

tasks:
  run-tests:
    deps:
      - task: lib:run-python
        vars:
          file: "3d_spada.py"
          cpus: 8
          args: "--reflevel 4 --DG0_layers 2"
