# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

includes:
  lib:
    taskfile: ../../.tasks.lib.yml
    internal: true

tasks:
  run-tests:
    deps:
      - task: lib:run-python
        vars:
          file: "3d_sphere_burgers.py"
          cpus: 8
          args: "--reflevel 4 --DG0_layers 3 --lateral_visc --viscosity_ratio 0.1"
